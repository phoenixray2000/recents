<Window x:Class="Recents.App.PreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Preview"
        Width="860" Height="640"
        MinWidth="400" MinHeight="300"
        WindowStyle="None"
        AllowsTransparency="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip">

    <!-- 外层阴影容器 -->
    <Border Margin="8"
            Background="{DynamicResource BgMain}"
            CornerRadius="10"
            BorderBrush="{DynamicResource WindowBorderBrush}"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.6" Color="{DynamicResource WindowShadowColor}"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>   <!-- 标题栏 -->
                <RowDefinition Height="*"/>    <!-- WebView2 内容区 -->
                <RowDefinition Height="28"/>   <!-- 底部状态栏 -->
            </Grid.RowDefinitions>

            <!-- ── 标题栏（可拖动）──────────────────────────── -->
            <Border Grid.Row="0"
                    Background="{DynamicResource BgTitleBar}"
                    CornerRadius="10,10,0,0"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="12,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- 文件图标（Fluent Icon 占位）-->
                    <TextBlock Grid.Column="0"
                               x:Name="FileIconText"
                               FontFamily="Segoe Fluent Icons"
                               FontSize="16"
                               Text="&#xE8A5;"
                               Foreground="{DynamicResource AccentBlue}"
                               VerticalAlignment="Center"
                               Margin="0,0,8,0"/>

                    <!-- 文件名 -->
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock x:Name="FileNameText"
                                   Text="—"
                                   Foreground="{DynamicResource TextPrimary}"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   TextTrimming="CharacterEllipsis"/>
                    </StackPanel>

                    <!-- 关闭按钮 -->
                    <Button Grid.Column="2"
                            x:Name="CloseBtn"
                            Content="&#xE8BB;"
                            FontFamily="Segoe Fluent Icons"
                            FontSize="14"
                            Foreground="{DynamicResource TextSecondary}"
                            Background="Transparent"
                            BorderThickness="0"
                            Padding="6,4"
                            Cursor="Hand"
                            Click="CloseBtn_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="bd" Background="Transparent" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="bd" Property="Background" Value="{DynamicResource ColorDanger}"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- ── WebView2 内容区 ────────────────────────────── -->
            <Grid Grid.Row="1">
                <!-- 加载中指示器（WebView2 初始化时可见）-->
                <TextBlock x:Name="LoadingText"
                           Text="Loading…"
                           Foreground="{DynamicResource TextSecondary}"
                           FontSize="14"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Visibility="Visible"/>

                <wv2:WebView2 x:Name="WebView"
                               Visibility="Collapsed"
                               DefaultBackgroundColor="Transparent"/>
            </Grid>

            <!-- ── 底部状态栏 ──────────────────────────────────── -->
            <Border Grid.Row="2"
                    Background="{DynamicResource BgTitleBar}"
                    CornerRadius="0,0,10,10"
                    BorderBrush="{DynamicResource WindowBorderBrush}"
                    BorderThickness="0,1,0,0">
                <Grid Margin="12,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock x:Name="FilePathText"
                               Grid.Column="0"
                               Foreground="{DynamicResource TextSecondary}"
                               FontSize="11"
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"/>
                    
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                        <TextBlock x:Name="FileMetaText"
                                   Foreground="{DynamicResource AccentBlue}"
                                   FontSize="11"
                                   VerticalAlignment="Center"/>
                        <TextBlock Text="  |  " Foreground="{DynamicResource BorderMuted}" FontSize="11" VerticalAlignment="Center" Margin="8,0"/>
                        <TextBlock Text="Enter to Open"
                                   Foreground="{DynamicResource TextTertiary}"
                                   FontSize="11"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
